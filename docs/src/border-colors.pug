extends /_includes/_layouts/core

block config
  - pageTitle = "Border colors"
  - pageClasses = data.tailwind.getClasses('colors', 'border');

block content
  .flcw.screenshot
    each className in pageClasses
      .fln.flrnw.border-01(class=className)
        code.fla.code=`.${className}`

